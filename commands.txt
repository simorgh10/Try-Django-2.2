== Setup the project ==

pipenv --python 3.8 install django==3.1.1
pipenv shell
mkdir src
cd ./src
django-admin startproject try_django .
python manage.py runserver
python manage.py migrate
python manage.py createsuperuser

(username guest password guest)

== View settings in shell ==

python manage.py shell
see settings
>>> from django.conf import settings
>>> settings.BASE_DIR

== context processors ==

Note :
default context variables are coming from settings context processords

'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],

Thus you can use {{user}}, or {{user.is_auhenticated}} as context variables

== Create a new app ==

python manage.py startapp blog
the add the app to INSTALLED_APPS

always after creating or modifying a model, run :
python manage.py makemigrations
python manage.py migrate

== Creating a new user ==

the password used for user author is authorauthor2

== Static files ==

to collect static files
python manage.py collectstatic
